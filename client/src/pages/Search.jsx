import React from "react";
import { Container, Search as SearchComponent} from "../components";

const Search = () => {
	return (
		<Container>
			<SearchComponent/>
		</Container>
	);
};

export default Search;
